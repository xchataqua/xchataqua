{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {CLASS = TabOrWindowView; LANGUAGE = ObjC; SUPERCLASS = NSView; }, 
        {
            ACTIONS = {"do_clear" = id; "do_save" = id; }; 
            CLASS = UrlGrabberWin; 
            LANGUAGE = ObjC; 
            OUTLETS = {"url_grabber_view" = TabOrWindowView; "url_list" = NSTableView; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}