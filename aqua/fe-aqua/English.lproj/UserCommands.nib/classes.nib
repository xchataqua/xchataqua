{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {"do_add" = id; "do_cancel" = id; "do_delete" = id; "do_ok" = id; }; 
            CLASS = UserCommands; 
            LANGUAGE = ObjC; 
            OUTLETS = {"cmd_list" = NSTableView; "cmd_text" = NSTextView; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}